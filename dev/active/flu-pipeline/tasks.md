# Tasks

Mirrors the session's live task list. Update whenever that list changes materially.

## Done

- [x] `common/` module (epiweek, sustained_crossing, http, disk_guard, io_jsonl) — 100% coverage.
- [x] `ground_truth/` module (FluNet client, UKHSA client, onset detection, builder) — 100% coverage, validated against live APIs for all 8 countries x 2015-2024. Output: `data/ground_truth/country_seasons.csv`. Real onset counts for the current 6-country GDELT cohort (see cohort-change entry below): USA 9, AUS 8, BRA 9, IND 10, KEN 8, IDN 10 (54 total).
- [x] `extraction/` module (schema, prompt, relevance prefilter, Groq client) — 100% coverage, validated against live Groq API.
- [x] `detection/`, `join/`, `mapping/`, `evaluation/` modules — 100% coverage against synthetic fixtures.
- [x] `gdelt/` module (window builder, manifest, raw downloader, gkg parser, flu filter, article fetcher) — validated against live GDELT (gkg_parser: 1142/1142 real lines parsed; FIPS country codes confirmed empirically: UK/JA/AS/KE/IN/US; a full real day reproduced the spike's known match count).
- [x] Step 2a hourly-subsampling calibration — ran, failed decisively (0-32% recall vs >=90% bar). Full 96-files/day method confirmed required.
- [x] Step 2 pilot ingestion — USA 2023 + KEN 2023, 170 unique days, 6,187 matched articles (6,179 USA, 8 KEN), ~2.06 hours.
- [x] Step 3 extraction for Kenya — 8/8 done (4 extracted, 2 skipped_prefilter, 2 not_retrievable).
- [x] Fixed avian/animal-flu misclassification in the extraction prompt (see `context.md`).
- [x] Corrected Groq rate-limit model (round 1): 1,000 RPD per model, confirmed per-model not account-wide, built `MultiModelExtractionClient` round-robin across gpt-oss-120b/gpt-oss-20b/qwen3.8-27b.
- [x] Corrected Groq rate-limit model (round 2, the real bottleneck): discovered a second, tighter per-model cap -- 200,000 tokens/day (TPD), hit before RPD given our prompt size. Shrunk `MAX_ARTICLE_CHARS` 6000->1500, tightened the few-shot prompt, added `max_tokens=300`. Measured ~78% token reduction (3372->741 tokens/call) via the API's own `usage` field.
- [x] Per-model quality spot-check (gpt-oss-120b vs gpt-oss-20b, on the same 6 USA articles, old vs new prompt) — no model looked systematically weaker; `relevant` agreed 6/6; minor severity/strain variance only. See `context.md`.
- [x] Full 6-country token/volume projection, flagged plainly: even with the shrunk prompt, a full 6-country run is **~69 days (~2.3 months)** of continuous daily extraction on the free tier (~51,000 estimated LLM calls at ~741/day pooled) -- was ~263 days before the fix. **Open decision for the team**: accept the timeline, scope down, or move to a paid tier/different provider. See `context.md`.
- [x] Dev docs set up: this file, `plan.md`, `context.md`, and root `CLAUDE.md`.
- [x] **Cohort change: dropped GBR and JPN, added BRA and IDN.** Team decision (not a content-quality call) -- GBR (UKHSA source, no FluNet records) and JPN (peak-count proxy, no specimen-processed denominator) were the only two countries with a non-uniform onset-detection method; dropped to keep one flat FluNet `positivity_2wk` threshold across the whole cohort, no per-country exceptions. BRA/IDN already used that same flat method and are added in their place, despite their confirmed-thinner GDELT content (see volume-check entry below) -- a disclosed limitation, not an oversight. `pipeline/config.py`'s `COUNTRIES` dict updated (`included_in_gdelt_cohort`: GBR/JPN -> False, BRA/IDN -> True); `data/ground_truth/country_seasons.csv` regenerated via `run_step1_ground_truth` to match (54/60 onsets found in the new cohort). GBR's and JPN's already-ingested matches are kept on disk (`data/gdelt_matched/{GBR,JPN}.jsonl`, 3,707 and 3,000 articles respectively, across their full defined-onset windows) but are no longer part of the active cohort or its reporting.
- [x] **Step 2 full-window volume-check ingestion for AUS/IND/BRA/IDN (all defined-onset seasons, 2015-2024)** -- `investigation/run_step2_remaining_countries.py`, own manifest (`data/gdelt_manifest/manifest_batch2.jsonl`) to avoid the shared manifest's country-blind resumability gap (see script docstring). 1,638 unique days, ~10.5 hours wall-clock across 3 runs (2 interrupted by unrelated system-wide OOM kills, both resumed cleanly from the manifest checkpoint with zero data loss).
- [x] **Step 2 full-window ingestion for USA/KEN's remaining seasons (2015-2024)** -- `investigation/run_step2_usa_ken.py`, own manifest (`data/gdelt_manifest/manifest_batch3.jsonl`), same country-blind-manifest rationale as batch2. USA: 8 remaining seasons (2015, 2016, 2017, 2018, 2019, 2020, 2022, 2024 -- 2021 has no onset found); KEN: 7 remaining seasons (2016, 2018, 2019, 2020, 2021, 2022, 2024 -- 2015 has no data, 2017 has no onset found). 1,075 unique days, ~19.2 hours wall-clock (one ~5-hour stall mid-run was the Mac going to system sleep, not a script hang -- `time.monotonic()`'s internal clock pauses across sleep while wall-clock elapsed doesn't, confirmed via `ps` ELAPSED vs the log's own elapsed field; fixed by running `caffeinate -dis -w <pid>` for the remainder, no data lost).

  **Found and fixed a real duplication bug**: USA's 2023 (pilot) and 2024 season windows overlap by ~8 weeks (onsets only 4 weeks apart -- 2023-12-04 and 2024-01-01). Since this script deliberately uses its own manifest (`manifest_batch3.jsonl`, separate from the pilot's `manifest.jsonl`) to avoid the country-blind resumability gap, the shared overlap days got downloaded and matched a second time against USA, writing 4,168 duplicate `(stamp, url)` lines into `data/gdelt_matched/USA.jsonl`. Deduplicated in place (pre-fix copy kept at `USA.jsonl.pre_dedup_backup`); post-dedup, the 2023 window's own membership count exactly recovers the original pilot figure (6,179), confirming the fix is correct and no real matches were lost. KEN has no such overlap (its 2023/2024 windows are ~8 months apart) and had zero duplicates to begin with; AUS/IND/BRA/IDN (single shared manifest, no cross-manifest overlap) were checked too and are also clean.

  **Final per-country-season breakdown, all 6 active-cohort countries, full 2015-2024 real data (no more single-season stand-ins):**
  | Country | Seasons (defined onset) | Total matched (unique) | Avg/season |
  |---|---|---|---|
  | USA | 9 (all ingested) | 58,458 | 6,495.3 |
  | KEN | 8 (all ingested) | 163 | 20.4 |
  | AUS | 8 (all ingested) | 10,451 | 1,306.4 |
  | IND | 10 (all ingested) | 4,255 | 425.5 |
  | BRA | 9 (all ingested) | 593 | 65.9 |
  | IDN | 10 (all ingested) | 216 | 21.6 |

  USA per-season detail (window = onset -10wk/+2wk): 2015 **0** (window 2014-10-20 to 2015-01-12 predates GDELT's 2015-02-19 coverage start entirely -- the same known limitation already documented for AUS/JPN's 2015 seasons, not new here), 2016 7,103, 2017 5,502, 2018 13,769, 2019 8,243, 2020 9,732, 2022 5,577, 2023 6,179, 2024 6,521 (2023/2024 window-membership counts legitimately overlap -- an article published in the shared 8-week span belongs to both seasons' own detection window; the 58,458 total is the deduplicated unique-article count, not a sum of these two).

  KEN per-season detail: 2016 3, 2018 12, 2019 5, 2020 2, 2021 4, 2022 10, 2023 8, 2024 **119** (not a manifest-duplication artifact -- checked for window overlap with adjacent KEN seasons, none found -- but see the wire-story contamination finding below: this spike is real matched-URL volume, not genuine Kenya-specific content).

  **AUS and IND read as content-rich** (same order of magnitude as USA's per-season average / far above KEN's thin reference). **BRA and IDN read as thin** (IDN only ~1x KEN's thin per-season average, BRA ~3x but still far below AUS/IND) -- consistent with the earlier 5-day extended-sample finding (33% retrievable vs the >=50%/60% bar), now confirmed with a full-set retrievability check (see next entry).

- [x] **BRA/IDN/KEN inclusion decision (resolved)** -- a full-set retrievability/content-depth check (`investigation/check_bra_idn_full_retrievability.py`, fetching all 809 already-matched BRA+IDN articles via the production `fetch_article_text`, not the earlier 10/day x 5-day sample of up to 100 articles) replaced the 33% estimate with real numbers: BRA 227/593 retrievable (**38.3%**), 193/593 content-rich (**32.5%**); IDN 100/216 retrievable (**46.3%**), 92/216 content-rich (**42.6%**); combined 327/809 retrievable (**40.4%**), 285/809 content-rich (**35.2%**). Both countries individually, and the combined figure, fall below **both** the project's >=60% retrievability and >=50% content-depth bars.

  **Kenya was initially left out of this check by default** (its numbers came from AUS/IND-adjacent reasoning, not its own data) -- caught before Step 3 extraction proceeded on Kenya's full backlog. Running the identical check (`investigation/check_ken_full_retrievability.py`) on all 163 KEN matched articles: 55 retrievable (**33.7%**), 36 content-rich (**22.1%**) -- below both bars, and thinner than BRA/IDN, not comparable to AUS/IND as had been assumed. (Live-fetch numbers vary by a handful of articles run-to-run; a repeat run saw 54/35 -- conclusion unchanged either way.)

  A follow-up investigation of *why* found a real content-matching contamination, distinct from the USA manifest-duplication bug above: **117 of Kenya's 163 total matched articles across the full 2015-2024 decade (71.8%) are the exact same January-2024 AP wire story about US hospital mask mandates**, independently syndicated across 100+ unrelated US/Canadian local-news URLs with zero Kenya content -- each is a genuinely distinct URL/GKG record (not a duplicate write), but GDELT's own location tagging attributed Kenya to all of them regardless. This story alone also accounts for 21 of Kenya's 36 nominal "content-rich" hits (its "hospitals... cases surge" language trips the keyword scorer). Of the 15 non-wire-story content-rich matches, only ~6 are genuinely Kenya-specific (`standardmedia.co.ke`, `businessdailyafrica.com`, `kenyans.co.ke`, `capitalfm.co.ke`) across the entire decade -- under one usable Kenya-specific article per season.

  Per the project's own pre-committed decision rule, applied without exception: **BRA, IDN, and KEN are all excluded from the content-based detection comparison** (Steps 3/6 run on the LLM-extracted content signal) **but remain in the frequency-count baseline** (raw regex-matched article counts, no LLM/content-depth requirement, so thinner usable-content doesn't disqualify them there) -- with Kenya's baseline series flagged as partly contaminated by the wire-story spike in early 2024. This resolves the "open decision" left by the volume-check entry above -- it is closed, not to be re-litigated in a future check. Full rationale and numbers also recorded in `context.md`.

- [x] **Geotagging cross-check built and validated** (previously-promised scope, not yet built): GDELT's V2Locations tagging is confirmed unreliable in 3 separate cases (Brazil spike sample, Kenya's 71.8% wire-story contamination, India's 132-article Australian bird-flu cluster). Built as a new `primary_country` field on the Step 3 extraction schema (`pipeline/extraction/schema.py`), compared against the GDELT tag via `pipeline/extraction/geo_crosscheck.py::classify_geo_match` (agree/ambiguous/disagree), wired into `run_step3_extract.py` -- disagreements are flagged (`status="excluded_geo_mismatch"`), never deleted, fully auditable/reversible. Validated against the 3 known cases via `investigation/validate_geo_crosscheck.py`: India's cluster caught at 89-94% (run-to-run LLM variance) across repeated runs, specificity guard on known-genuine outlets clean at 0-7% false positives. Two real bugs found and fixed along the way (see context.md): Groq's structured-output mode rejected the initial 3-way schema shape, and the new field's harder judgment call blew the fixed completion-token budget via hidden reasoning tokens, both fixed. Kenya's case validated less cleanly, for a reason unrelated to the cross-check itself -- see context.md's "article-fetcher boilerplate-extraction" entry.

  **CORRECTED 2026-09-18** (see context.md's "Data-quality audit and fix cycle"): the audit-scale heuristic re-run cited below this line as of the previous session (`investigation/geo_heuristic_recheck.py`'s original `geo_mentions_country`, a bare keyword-presence check) was tested directly against the known India/Australia bird-flu cluster and **failed to catch it** (all 4 tested URLs returned a false "mentions India" positive). The "India remains the only country clearing both bars, 61.6%/56.1%" claim this entry previously made was based on that flawed heuristic and is **superseded, not accurate**. Replaced with a validated comparative version (`geo_matches_comparative`, 4/4 known-mismatch catch rate, 22/22 specificity-guard pass rate -- see context.md item 9) and re-run as the Wave 1 audit (context.md item 10): corrected numbers are **USA 47.4%/33.0%, AUS 45.7%/22.0%, IND 61.1%/35.6%** -- IND still clears retrievability but **none of the three clear the content-rich bar** anymore. This is the current, correct picture; see the new blocking item below.
- [ ] **Open backfill decision**: USA's existing 218 and KEN's existing 8 Step 3 extraction records predate the `primary_country` field and were not regenerated in this pass (touching existing production extraction data needs an explicit go-ahead, not a unilateral rewrite) -- AUS/IND's zero prior records mean their first real run will include the field with no backfill needed.
- [x] **Data-quality audit and fix cycle (2026-09-18)** -- full detail in `context.md`'s "Data-quality audit and fix cycle" section. Summary of what was built/fixed/run:
  - Found and fixed the missing `User-Agent` header in `get_with_retry` (`pipeline/common/http.py`) that was silently causing several mainstream outlets to block/stub fetches.
  - Re-audited the original 8-country spike's 88%/88% figure and found it compromised (USA slice 9/10 one wire story; 2/34 samples mistagged to the wrong country) -- flagged as directionally encouraging only, not a reliable baseline.
  - Audited USA's matched set at production scale for the first time (41.7%/38.3%, later re-run at 46.6%/42.75% -- see context.md item 3, not fully reconciled).
  - Re-checked GBR/JPN for content-comparison re-inclusion (closing the option (b) left open below): GBR 46.0%/40.9%, JPN 38.4%/31.9%, neither clears the bar.
  - Rebuilt the content-richness heuristic (`pipeline/gdelt/content_richness.py`) to replace the unreliable 200-char/keyword-blocklist approach with a combined structural score; validated against the 148 known AUS false positives (113/144 fixed, remainder traced to a pre-existing `CONTENT_MARKERS` gap, not a regression).
  - Investigated whether the English-only, URL-only flu-term filter was suppressing non-English coverage for BRA/IND/KEN. Found BRA has essentially zero genuine domestic Brazilian press in GDELT's candidate pool at all (a geo-tagging/candidate-pool gap, confirmed across the full 2015-2024 window via a 9-date sample, not a term-matching gap or a sampling fluke). Found IND has genuine domestic press but uses English-language URL slugs regardless of source language (confirmed against a major Hindi outlet, `post.jagran.com`); a title/body-fetch fix was cost-estimated at 3.6-5.4 days for IND alone and ruled infeasible for this checkpoint. Term-matching fix (Phase 1) dropped entirely as a result.
  - Found and fixed a real flaw in the audit-scale geo-heuristic: the existing presence-only check failed its own best-known test case (the India/Australia cluster). Built and validated a comparative replacement (`geo_matches_comparative`, 4/4 known-mismatch catch, 22/22 specificity-guard pass).
  - Ran the Wave 1 audit (USA, AUS, IND) with both fixes applied. Found real, large-scale geo-mismatch contamination beyond the known cases -- 45% of AUS's nominal content-rich set is US/UK/Canadian domestic flu coverage mistagged to Australia via an "Australia's flu season predicts ours" journalistic framing pattern, not the isolated case previously assumed. Final corrected numbers: USA 33.0% content-rich, AUS 22.0%, IND 35.6% -- all below the 50% bar. **None of the three remaining content-comparison countries currently clear both pre-registered bars.**

- [ ] **NEW BLOCKING ITEM, top priority: none of USA/AUS/IND currently clear both quality bars post-fix; team decision needed before any further Step 3/6 work.** BRA and KEN are confirmed (not just deprioritized) structurally unfixable within this checkpoint window -- BRA has no genuine domestic press in GDELT's candidate pool at all, KEN has real but thin domestic press dominated by wire-story syndication contamination -- both stay excluded from content comparison and in the frequency baseline only, per the already-resolved decision above. The open question is what to do now that the *remaining* cohort (USA/AUS/IND) also fails the content-rich bar after the corrected geo-crosscheck (context.md item 10): **(a)** revisit the >=50% content-rich bar itself as an explicit, disclosed decision (e.g. lower it, or redefine "content-rich" now that geo-mismatch exclusion is doing real work the bar wasn't calibrated against), **(b)** proceed with the data as-is and report the limitation honestly in the final writeup (weaker content-signal-vs-frequency-baseline comparison than hoped, disclosed plainly), or **(c)** pursue a different mitigation not yet identified. Not decided in this session -- this is now the single most important open item blocking further pipeline work, superseding the <40-unit cohort-size item below in urgency (that item is still open too, and compounds this one: the cohort is both too small *and* now confirmed too thin in content-richness).

- [ ] **NEW BLOCKING ITEM: content-comparison cohort has dropped below the project's own >=40-unit bar.** With BRA (9), IDN (10), and now KEN (8) all excluded from content comparison, only **USA (9) + AUS (8) + IND (10) = 27 country-season units** remain for Step 3/6 content-signal work -- below the project's pre-committed >=40-unit go/no-go bar (`investigation/report.md`, `investigation/updated_problem_statement.md`). This is a materially different situation from the BRA/IDN-only exclusion (which still left 35 units, itself short of 40 but not yet flagged as decisively as this). Needs an explicit team decision among: (a) accept the 27-unit cohort and disclose a narrower-than-scoped limitation; (b) reconsider GBR/JPN for content-comparison specifically -- they were dropped for onset-method uniformity with the frequency-baseline countries, not content quality, and both already have full-window matched articles on disk (GBR 3,707, JPN 3,000) from before the cohort change, so no new Step 2 ingestion would be needed to re-include them for this track only; (c) lean the write-up more heavily on IND (the largest remaining block) with small-cohort caveats on USA/AUS. See `context.md` for the same flag.

## Paused -- checkpointed cleanly, ready to resume

- [x] Step 3 extraction for USA -- stopped cleanly (not killed mid-write) at **218 of 6,179 matched articles processed** (143 extracted, 67 not_retrievable, 2 skipped_prefilter, 6 failed; 149 total LLM calls across the pool: 43 gpt-oss-120b, 58 gpt-oss-20b, 1 qwen3.8-27b, plus ~47 earlier calls made before per-model tracking existed). Checkpoint verified valid: `data/extractions/USA.jsonl` (218 lines, 0 malformed, loads cleanly via `ExtractionStore`), `data/gdelt_manifest/manifest.jsonl` (16,320 lines, 0 malformed), `data/gdelt_matched/{USA,KEN}.jsonl` untouched (6,179 + 8). Resume anytime with `python -m pipeline.cli.run_step3_extract --country USA` -- no flags needed, it skips everything already done. Whatever infra the team decision below lands on, this checkpoint is valid to continue from as-is.

## BLOCKING: team decision needed before further pipeline work

- [ ] **Free-tier TPD caps make the full 6-country Step 3 run ~2.3 months even with the token-shrunk prompt (~51,000 estimated LLM calls at ~741/day pooled across 3 models).** Work is paused pending a decision among:
  - **(a) Local GPU inference** via Ollama / a quantized open model, matching Health Sentinel (Pant et al. 2025)'s own approach -- no API rate limits at all, but needs local GPU capacity and a new `ExtractionClient` implementation.
  - **(b) Groq's paid Dev Tier** -- pay-as-you-go, no subscription/minimum spend, removes the RPD/TPD caps entirely. **Cost estimate computed and ready** (see `context.md`): **~$3.89 (gpt-oss-20b) to ~$7.79 (gpt-oss-120b, current default) for the entire full 6-country run** — strikingly cheap, though pricing was sourced from third-party aggregators (Groq's own pricing page is JS-rendered and didn't return content via direct fetch) and should be confirmed in the Groq console before committing budget.
  - **(c) Tighter upstream relevance prefiltering** to cut LLM call volume before it ever reaches the model -- reduces cost/time on any tier, but trades off recall (some genuinely relevant articles would be filtered out before the LLM ever sees them); needs the tradeoff quantified before adopting.
  - This is not resolved here -- it's the team's call. Pilot-scale work (Steps 5-8 on USA 2023 + KEN 2023, using what's already extracted) can proceed independently of this decision if useful; full-scale work should not resume until it's made.

## Open (blocked on the decision above, or on pilot Steps 5-8)

- [ ] Step 5 join (USA 2023 + KEN 2023 pilot) -- can run now on the 218 USA + 8 KEN records already extracted, doesn't need the full USA backlog.
- [ ] Step 6 detection/lead-time (pilot) — the actual first real lead-time number for this project.
- [ ] Step 7 mapping (pilot, trivial for 2 countries).
- [ ] Step 8 evaluation (pilot) — lead-time distribution + retrievability vs the spike's 88%/88%.
- [ ] Full-scale Step 3 extraction — all 6 active-cohort countries now fully ingested for Step 2 (see Done section); extraction itself is still blocked on the team decision above. Note BRA/IDN/KEN are all excluded from this content-based extraction/detection track per the resolved decision above -- only **USA/AUS/IND** feed Step 3+ (27 country-season units total; see the new blocking item above on the <40-unit cohort). Kenya's earlier pilot Step 3 extraction (8/8 articles, see Done section above) predates this decision and was useful for validating the extraction pipeline itself, but its output should not be treated as part of the final content-comparison cohort.
- [ ] Full-scale Steps 5-8, final evaluation report with limitations section.
- [ ] Dengue secondary track — not started, paused.
